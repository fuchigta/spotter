# 免除頻度・ノイズ量の測り方

`spotter-refactor` の手順3・4の具体的なコマンド例です。

## 免除トレーラの濫用頻度を測る

免除トレーラは `<トレーラ名>: skip <理由>`（大文字小文字を区別しない）という形式です
（`internal/exempt/exempt.go` の実装を参照）。直近のコミットメッセージから、トレーラ名
ごとの出現回数を数えます。

```bash
git log --format=%B -200 | grep -ioE '^[a-z][a-z0-9-]*: *skip ' | sort | uniq -c | sort -rn
```

出力例:

```
  8 Diff-Content: skip
  3 Doc-Sync: skip
  1 Commit-Intent: skip
```

**この数字だけでは判断できません。** 同じ検査でも「毎回違う正当な理由で免除されている」
のか「実質的に同じ理由を毎回書かされている」のかで意味が違います。頻度が高い検査は、
実際の免除理由（`git log --format=%B -50 --grep='^Diff-Content: skip'` のように絞り込んで
本文を読む）を確認してください。同じような理由が繰り返し出てくるなら、それは
`.spotter.yml` 側の対象・除外パターンで先回りして潰せるはずのパターンです。

`-200` は目安です。リポジトリのコミット頻度に応じて調整してください（コミット数が
少ないリポジトリでは全履歴でも構いません）。

## 検査のノイズ量を測る

過去のコミット・現在のワークツリーに対して実際に検査を走らせ、どの検査が何回落ちるかを
数えます。

```bash
spotter check --range HEAD~50..HEAD 2>&1 | grep -oE '^[a-z][a-z0-9-]* の検査に失敗しました' | sort | uniq -c | sort -rn
```

`worktree` 粒度の検査（`doc-paths`/`consistency`/`doc-links`）は `--range` の値に
関わらず現在のワークツリーを1回だけ見るため、この方法では「今落ちているか」の
0/1 しか分かりません。頻度ではなく `spotter config lint` の Finding と併せて
判断してください。

`per-commit`/`squashed` 粒度の検査で頻度が高い場合、免除頻度が低いのに検査自体は
頻繁に落ちている（＝毎回コードやコミットメッセージを直して通している）パターンと、
免除頻度も検査の失敗頻度も両方高い（＝ほぼ免除で通している）パターンを区別してください。
前者は検査が機能している証拠なので触る必要はありません。後者は検査の対象・基準が
実態と合っていない兆候です。

## 変更案の根拠をまとめる例

手順6でユーザーに提示する際は、検査ごとに次の形でまとめると判断しやすくなります。

```
- diff-content: 直近50コミット中 8 回失敗、うち 8 回とも免除で通過
  → 免除理由は全て「外部ライブラリの型定義が壊れている」
  → 該当パターン（@ts-ignore の deny）に paths で対象を絞れば解決しそう
  → 提案: deny[].paths を 'src/**/*.ts' に限定する（修正）

- doc-sync-legacy: spotter config lint で pairs[0].paths が現在のワークツリーに
  一致しないと報告された
  → 対応するディレクトリが internal/legacy/ から internal/core/ にリネーム済み
  → 提案: pairs[0].paths を internal/core/*.go に更新する（修正）

- consistency-old-versions: spotter config lint で sources[1].file が存在しない
  と報告された。対応するファイル自体が削除されている
  → 提案: このチェック全体を削除する（削除）
```
