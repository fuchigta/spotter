# spotter

コーディングエージェントのコミットを、手元のフックと CI で同じ基準の検査にかけるツールの用語集です。
コードの型名が対応する語には括弧で添えています。

## 人と約束

**利用者**:
spotter を自分のリポジトリに導入する人やチーム。エージェントを操作している人もここに含む。
_Avoid_: ユーザー

**コーディングエージェント**:
spotter が主に支える相手で、コミットを作る主体。

**約束**:
spotter が利用者に対して守ると決めている振る舞い。変えるときは破壊的変更として扱う。
_Avoid_: 契約（command 型の「入出力契約」は別の概念で、そのまま使う）

## 検査

**検査**:
`.spotter.yml` の `checks.<キー>` 1 件として設定された、実行される検査のまとまり。キーが名前になる。
_Avoid_: 検査インスタンス、チェック

**検査 type**:
検査の種類。組み込み type と command 型に分かれ、同じ検査 type から複数の検査を作れる。
_Avoid_: 検査の種類（見出し語として）

**組み込み type**:
spotter 本体が実装を持つ検査 type。

**command 型**:
`types` に登録した外部コマンドで違反を判定する検査 type。
_Avoid_: 外部コマンド検査

**起動粒度**（`Granularity`）:
range モードで検査をどの単位で起動するか。`squashed` / `per-commit` / `worktree` のいずれかで、検査 type ごとに固定。
_Avoid_: 粒度、granularity（設定キー以外で）

**起動**:
検査が 1 回実行される単位。起動粒度によって、範囲全体で 1 回、コミットごとに 1 回、作業ツリーに 1 回のいずれかになる。
_Avoid_: 1 回の比較

**終点**:
1 回の起動が見る比較の終わり側。staged モードではインデックス、range モードではその起動が見る最新のコミット。`worktree` 粒度の起動には終点が無い。

**作業ツリー**（`FS`）:
`worktree` 粒度の起動が見る、リポジトリのルート以下の現在のファイル。検査には `fs.FS` として渡す。
_Avoid_: ワークツリー

## モード

**staged モード**:
これから作るコミット（ステージ済みの変更とメッセージ）を検査する実行のしかた。commit-msg フックで使う。

**range モード**:
既存のコミットの範囲を検査する実行のしかた。CI で使う。
_Avoid_: 範囲モード

**範囲**:
range モードで検査するコミットの並び。マージコミットは含まない。「範囲」は常にこの意味で使う。

`worktree` はモードではなく起動粒度の値である。

**検査対象外**:
マージコミットや差分の無いコミットのように、検査が初めから見ないもの。免除とは別物。
_Avoid_: スキップ

## 結果

**違反**（`Violation`）:
検査が見つけた 1 件の問題。重大度は持たない。

**不合格**:
免除されていない違反が 1 件以上ある検査の結果。利用者向けの表示では「失敗」とも言う。

**合格**:
免除されていない違反が 1 件も無い検査の結果。合格と不合格の間の段階は無い。
_Avoid_: 警告

**実行エラー**:
設定の誤りなどで検査そのものが走れないこと。不合格とは別物。

## 免除

**免除**:
理由を添えたトレーラで、検査の違反を許すこと。`skip` はトレーラ中のキーワードにすぎない。
_Avoid_: スキップ

**免除トレーラ**:
`<トレーラ名>: skip <理由>` の形の 1 行。トレーラ名は既定ではキーから作られる。

**トレーラ段落**:
コミットメッセージの最後の段落で、全ての行がトレーラの形をしているもの。免除トレーラはここにあるときだけ効く。

**全体免除**:
検査の違反を全て許す免除。

**スコープ付き免除**（`ScopedExemptable`）:
免除対象を列挙し、それに該当する違反だけを許す免除。対応しているかは検査 type ごとに決まる。
_Avoid_: 範囲付き免除、部分免除

**免除対象**（`Target`）:
スコープ付き免除で指定できる、検査の一部を表す名前（doc-sync ならドキュメントのパス）。違反はそれぞれ高々 1 つの免除対象に属する。

## 設定とスキル

**レシピ**:
ドキュメントに置いた設定例。既定値ではなく、利用者が自分の設定に写して使う。
_Avoid_: 既定値、プリセット

**同梱スキル**:
spotter が配布し、設置と更新を管理するコーディングエージェント向けのスキル。
